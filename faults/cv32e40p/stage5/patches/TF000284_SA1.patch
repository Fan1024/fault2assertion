--- a/cv32e40p.mapped.v
+++ b/run-local/TF000284_SA1/fault_netlist.v
@@ -34910,6 +34910,9 @@
      m_exc_vec_pc_mux_i, u_exc_vec_pc_mux_i, csr_mtvec_init_o,
      jump_target_id_i, jump_target_ex_i, hwlp_jump_i, hwlp_target_i,
      halt_if_i, id_ready_i, if_busy_o, perf_imiss_o);
+  // Fault2Assertion Stage-5 fault TF000284_SA1: temporary pre-fault net declared before first use
+  wire f2a_pre_tf000284_sa1;
+
   input clk, rst_n, req_i, instr_gnt_i, instr_rvalid_i, instr_err_i,
        instr_err_pmp_i, clear_instr_valid_i, pc_set_i, hwlp_jump_i,
        halt_if_i, id_ready_i;
@@ -34991,7 +34994,7 @@
   wire n_184, n_185, n_186, n_187, n_188, n_189, n_190, n_191;
   wire n_192, n_193, n_194, n_195;
   cv32e40p_aligner aligner_i(.clk (clk), .rst_n (rst_n), .fetch_valid_i
-       (fetch_valid), .aligner_ready_o (aligner_ready), .if_valid_i
+       (fetch_valid), .aligner_ready_o (f2a_pre_tf000284_sa1), .if_valid_i
        (if_valid), .fetch_rdata_i (fetch_rdata), .instr_aligned_o
        (instr_aligned), .instr_valid_o (instr_valid), .branch_addr_i
        ({branch_addr_n[31:1], UNCONNECTED_HIER_Z567}), .branch_i
@@ -35711,6 +35714,9 @@
        (n_184));
   XOR2_X1 \add_167_42/g787 (.A (\add_167_42/n_2 ), .B (pc_id_o[12]), .Z
        (n_176));
+
+  // Fault2Assertion Stage-5 fault TF000284_SA1: run-local stuck-at assignment
+  assign aligner_ready = 1'b1;
 endmodule
 
 module cv32e40p_load_store_unit_PULP_OBI0(clk, rst_n, data_req_o,
