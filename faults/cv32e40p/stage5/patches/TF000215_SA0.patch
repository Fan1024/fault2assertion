--- a/cv32e40p.mapped.v
+++ b/run-local/TF000215_SA0/fault_netlist.v
@@ -29214,6 +29214,9 @@
      mhpmevent_branch_taken_o, mhpmevent_compressed_o,
      mhpmevent_jr_stall_o, mhpmevent_imiss_o, mhpmevent_ld_stall_o,
      mhpmevent_pipe_stall_o, perf_imiss_i, mcounteren_i);
+  // Fault2Assertion Stage-5 fault TF000215_SA0: temporary pre-fault net declared before first use
+  wire f2a_pre_tf000215_sa0;
+
   input clk, clk_ungated_i, rst_n, scan_cg_en_i, fetch_enable_i,
        instr_valid_i, is_compressed_i, illegal_c_insn_i,
        branch_decision_i, is_fetch_failed_i, ex_ready_i, wb_ready_i,
@@ -29673,7 +29676,7 @@
   cv32e40p_controller_COREV_CLUSTER0_COREV_PULP0_FPU0 controller_i(.clk
        (clk), .clk_ungated_i (clk_ungated_i), .rst_n (rst_n),
        .fetch_enable_i (fetch_enable_i), .ctrl_busy_o (ctrl_busy_o),
-       .is_decoding_o (n_924), .deassert_we_o (deassert_we),
+       .is_decoding_o (n_924), .deassert_we_o (f2a_pre_tf000215_sa0),
        .illegal_insn_i (illegal_insn_dec), .ecall_insn_i
        (ecall_insn_dec), .dret_insn_i (dret_insn_dec), .mret_dec_i
        (mret_insn_dec), .uret_dec_i (uret_dec), .dret_dec_i (dret_dec),
@@ -32489,6 +32492,9 @@
        (n_1168));
   XOR2_X1 g297(.A (instr_rdata_i[18]), .B (regfile_waddr_ex_o[3]), .Z
        (n_1169));
+
+  // Fault2Assertion Stage-5 fault TF000215_SA0: run-local stuck-at assignment
+  assign deassert_we = 1'b0;
 endmodule
 
 module cv32e40p_aligner(clk, rst_n, fetch_valid_i, aligner_ready_o,
