--- a/cv32e40p.mapped.v
+++ b/run-local/TF000567_SA0/fault_netlist.v
@@ -34830,7 +34830,7 @@
        (trans_valid), .trans_ready_i (trans_ready), .trans_addr_o
        (trans_addr), .resp_valid_i (instr_rvalid_i), .fetch_ready_i
        (fetch_ready_i), .fetch_valid_o (fetch_valid_o), .fifo_push_o
-       (fifo_push), .fifo_pop_o (fifo_pop), .fifo_cnt_i (fifo_cnt),
+       (f2a_pre_tf000567_sa0), .fifo_pop_o (fifo_pop), .fifo_cnt_i (fifo_cnt),
        .fifo_empty_i (fifo_empty));
   MUX2_X1 g817(.A (fifo_rdata[27]), .B (instr_rdata_i[27]), .S
        (fifo_empty), .Z (fetch_rdata_o[27]));
@@ -34896,6 +34896,10 @@
        (fifo_empty), .Z (fetch_rdata_o[0]));
   MUX2_X1 g848(.A (fifo_rdata[22]), .B (instr_rdata_i[22]), .S
        (fifo_empty), .Z (fetch_rdata_o[22]));
+
+  // Fault2Assertion Stage-5 fault TF000567_SA0 run-local driver split
+  wire f2a_pre_tf000567_sa0;
+  assign fifo_push = 1'b0;
 endmodule
 
 module
